# OSCAR Continued Pretraining 数据处理与运行记录

日期：2026-06-11

## 1. 目标

使用已有 OSCAR BERT MLM 权重，在重构后的 Synthea 36 个月 pre-index 数据上继续进行 Masked Language Modeling（continued pretraining），之后用于分类任务微调和推理。

OSCAR 原始模型目录：

```text
oscar_omop/
├── config.json
├── model.safetensors
└── vocab_oscar.txt
```

原始重构数据：

```text
aiml/datasets/reconstruct_training.parquet
```

## 2. 原始数据检查

数据共有 28,429 条患者记录和 17 列，训练使用的主要列为：

```text
person_id
sorted_event_tokens
day_position_tokens
lookback_start_day
COHORT
```

最初发现 `sorted_event_tokens` 和 `day_position_tokens` 虽然内容看起来像数组，实际 Parquet 类型却是字符串：

```text
sorted_event_tokens: "['AGE:33' 'GENDER:F' 'RACE:white' ...]"
day_position_tokens: "[0 0 0 10464 10476 ...]"
```

当前项目的 `CustomDataset` 需要真正的数组列。若直接输入字符串，代码会把字符串逐字符处理，随后在 `PretrainingDataCollator` 中因 token 数量和 position 数量不一致而触发 `AssertionError`。

## 3. 将字符串列转换为数组列

使用脚本：

```text
aiml/convert_reconstruct_arrays.py
```

转换后的 Parquet schema：

```text
sorted_event_tokens: list<string>
day_position_tokens: list<int64>
```

该步骤原地修复了：

```text
aiml/datasets/reconstruct_training.parquet
```

验证结果：

```text
记录数                         28,429
空序列                         0
token/position 长度不一致      0
负数 position                  0
非单调 position                0
person_id 重复                 0
```

项目原生 `MedbertDataPreprocessor`、`CustomDataset` 和 `PretrainingDataCollator` 均可正常读取转换后的数组。

## 4. OSCAR 与 Synthea 词表差异

原始词表规模：

```text
Synthea vocab                  1,479
OSCAR vocab                   76,681
直接字符串交集                    26
直接 Synthea 覆盖率             1.76%
```

主要问题不是只有 code 不同，两边的 ontology 前缀格式也不同：

```text
Synthea             OSCAR
ICD-10-CM:          ICD10CM:
SNOMED-CT:          SNOMED:
RxNorm_drug:        RXNORM:
```

人口学 token 的大小写也不同，例如：

```text
RACE:white          RACE:White
RACE:black          RACE:Black
ETHNICITY:hispanic  ETHNICITY:Hispanic
ETHNICITY:unknown   ETHNICITY:Unknown
```

只统一三个医学前缀后，Synthea 对 OSCAR 的 unique token 覆盖率从 1.76% 提高到约 32.79%。

## 5. 将 Synthea token 规范化为 OSCAR 格式

使用脚本：

```text
aiml/normalize_tokens_for_oscar.py
```

转换规则：

```python
ICD-10-CM:   -> ICD10CM:
SNOMED-CT:   -> SNOMED:
RxNorm_drug: -> RXNORM:

RACE:white          -> RACE:White
RACE:black          -> RACE:Black
RACE:asian          -> RACE:Asian
RACE:other          -> RACE:Other
ETHNICITY:hispanic  -> ETHNICITY:Hispanic
ETHNICITY:unknown   -> ETHNICITY:Unknown
```

生成文件：

```text
aiml/datasets/reconstruct_training_oscar_normalized.parquet
```

该文件只修改了 `sorted_event_tokens`。患者 ID、标签、事件时间和其余辅助列均保持不变。

验证结果：

```text
记录数                         28,429
token/position 长度不一致      0
旧 ICD-10-CM: 前缀残留         0
旧 SNOMED-CT: 前缀残留         0
旧 RxNorm_drug: 前缀残留       0
```

注意：规范化只处理命名格式。类似 `ICD-10-CM:103697008` 这种疑似 ontology 标注错误的代码不会被自动改成 SNOMED，需要单独的 ontology mapping 或人工清洗。

## 6. 重建并扩展 OSCAR vocab

先从规范化数据生成候选词表：

```text
aiml/datasets/vocab_reconstruct_oscar_normalized.txt
```

该词表包含 1,170 个 token。

随后使用：

```text
aiml/expand_vocab.py
```

将 OSCAR 中不存在的 token 追加到 OSCAR 原词表末尾，生成：

```text
oscar_omop/vocab_oscar_reconstruct_expanded.txt
```

结果：

```text
OSCAR 原 vocab                 76,681
规范化数据候选 token            1,170
新增 token                        777
扩展后 vocab                   77,458
```

扩展时严格保留 OSCAR 原有顺序：

```text
ID 0 到 76680：保持原 token 和原 ID 不变
ID 76681 到 77457：追加新的 Synthea token
```

校验结果：

```text
OSCAR 原 token ID 保持不变      True
扩展词表重复 token              0
规范化数据 token 覆盖率         100%
```

## 7. Position ID 不兼容问题

OSCAR 配置为：

```json
{
  "max_position_embeddings": 1825,
  "position_embedding_type": "relative_key_query"
}
```

因此合法 position ID 范围是：

```text
0 到 1824
```

原始重构数据的 `day_position_tokens` 是患者完整时间轴上的累计天数，最大值为 18,229，不能直接输入 OSCAR 模型。

数据已经包含：

```text
index_day
lookback_start_day
lookback_months = 36
```

`lookback_start_day` 的计算方式是：

```python
lookback_days = round(lookback_months * 365.25 / 12)
lookback_start_day = max(0, index_day - lookback_days)
```

36 个月约为 1,096 天。例如：

```text
index_day          = 11541
lookback_start_day = 11541 - 1096 = 10445
```

事件 position 被转换成窗口内相对天数：

```python
relative_position = original_position - lookback_start_day + 1
```

原 position 为 `0` 的人口学 token 继续保持 `0`，把 `0` 保留为静态 demographic position。示例：

```text
原始 position       10464  10476  10632  11521
窗口起点            10445
相对 position          20     32    188   1077
```

使用脚本：

```text
aiml/rebase_day_positions.py
```

生成最终训练数据：

```text
aiml/datasets/reconstruct_training_oscar_relative.parquet
```

最终验证：

```text
记录数                         28,429
序列长度范围                    5 到 471
最大序列长度                      471
position 范围                  0 到 1096
token/position 长度不一致      0
非单调 position                0
```

配置采用 `override_maxlen: 512`。最长原序列 471，加上 loader 自动添加的 `[CLS]` 后也不会超过 512。

## 8. 修改预训练代码以加载 OSCAR weights

修改文件：

```text
src/pretrain_bert.py
```

原代码只通过 `BertConfig` 随机初始化新模型，不能进行 OSCAR continued pretraining。

现在当配置包含 `pretrained_model_dir` 时，会加载已有 MLM checkpoint：

```python
AutoModelForMaskedLM.from_pretrained(pretrain_config["pretrained_model_dir"])
```

如果模型原 embedding 数量与扩展 vocab 大小不同，则执行：

```python
model.resize_token_embeddings(len(vocabulary))
```

Embedding 变化：

```text
原 word embedding shape        [76681, 768]
新 word embedding shape        [77458, 768]
```

OSCAR 原有 76,681 行 embedding 保留，新追加的 777 行 embedding 由 Transformers 初始化，并在 Synthea MLM continued pretraining 中学习。

MLM 输出层也同步从 76,681 类扩大到 77,458 类。

## 9. 新增训练配置

配置文件：

```text
aiml/configs_aiml/pretrain_oscar_reconstruct.yaml
```

关键配置：

```yaml
training_data_filepath: "datasets/reconstruct_training_oscar_relative.parquet"
vocab_filepath: "../oscar_omop/vocab_oscar_reconstruct_expanded.txt"
pretrained_model_dir: "../oscar_omop"
output: "outputs/pretrain_oscar_reconstruct/"

override_maxlen: 512
max_position_embeddings: 1825
batch_size: 16
eval_batch_size: 16
num_training_epochs: 5
learning_rate: 0.00005
fp16: true
```

启动脚本：

```text
aiml/run_pretrain_oscar_reconstruct.sh
```

## 10. Dry Run 验证

在启动完整训练前，使用一条真实患者记录完成了：

```text
加载 OSCAR model.safetensors
加载扩展 vocab
resize token embeddings
构造 CustomDataset
执行 PretrainingDataCollator
执行 BertForMaskedLM 前向传播
计算 MLM loss
```

结果：

```text
embedding resize              76681 -> 77458
input shape                   (1, 512)
position range                0 到 1077
logits shape                  (1, 512, 77458)
loss finite                   True
```

说明数据、词表、embedding 大小和模型前向传播之间已经连通。

## 11. 远程环境和训练启动

远程项目路径：

```text
/data/dwang/Clinical-BERT-Training
```

使用 Conda 环境：

```text
sanofi
```

GPU：

```text
GPU 0: NVIDIA RTX PRO 6000, 约 96 GB
GPU 1: NVIDIA RTX PRO 6000, 约 96 GB
```

训练在 tmux 会话中运行：

```text
oscar-pretrain
```

启动命令：

```bash
conda activate sanofi
cd /data/dwang/Clinical-BERT-Training/aiml
mkdir -p outputs

CUDA_VISIBLE_DEVICES=0 bash run_pretrain_oscar_reconstruct.sh \
  2>&1 | tee outputs/pretrain_oscar_reconstruct.log
```

训练启动时的数据划分：

```text
Train: 28,144
Validation: 285
Total training steps: 8,795
```

训练已正常进入 step 更新阶段。

查看日志：

```bash
tail -f /data/dwang/Clinical-BERT-Training/aiml/outputs/pretrain_oscar_reconstruct.log
```

重新进入 tmux：

```bash
tmux attach -t oscar-pretrain
```

## 12. 当前警告与待确认事项

加载 OSCAR checkpoint 时出现：

```text
bert.encoder.layer.{0...11}.attention.self.distance_embedding.weight | UNEXPECTED
```

这表示当前 Transformers 加载得到的模型模块没有接收 checkpoint 中的 `distance_embedding.weight`。虽然训练能够继续、前向传播和 loss 均正常，但这些相对位置权重可能没有被恢复。

OSCAR 配置声明：

```text
position_embedding_type = relative_key_query
```

因此该警告不应简单当作无关警告。训练完成前后应进一步核对：

1. OSCAR checkpoint 最初使用的 Transformers 版本和自定义模型代码。
2. 当前 `sanofi` 环境中的 Transformers 版本。
3. 加载后每层 attention 模块是否实际包含 `distance_embedding`。
4. 是否需要固定到 OSCAR 原训练版本（配置记录为 Transformers 4.49.0）。

另有一个特殊 token 约定问题：

```text
OSCAR vocab ID 0 = [UNK]
OSCAR config pad_token_id = 0
```

即 OSCAR 原训练约定实际上让 ID 0 同时承担配置中的 padding ID。当前项目 collator 也用 `0` 填充 batch，因此 continued pretraining 保持了 OSCAR 原 ID 体系，没有插入或重排 `[PAD]`。扩展词表中的新 `[PAD]` 位于追加区，但当前 padding 仍使用 ID 0。后续若改 tokenizer/collator，需要明确这一约定，不能直接假设新增 `[PAD]` 的 ID 是实际 padding ID。

## 13. 本次新增或修改的文件

```text
src/pretrain_bert.py

aiml/convert_reconstruct_arrays.py
aiml/normalize_tokens_for_oscar.py
aiml/rebase_day_positions.py
aiml/expand_vocab.py
aiml/configs_aiml/pretrain_oscar_reconstruct.yaml
aiml/run_pretrain_oscar_reconstruct.sh

aiml/datasets/reconstruct_training.parquet
aiml/datasets/reconstruct_training_oscar_normalized.parquet
aiml/datasets/reconstruct_training_oscar_relative.parquet
aiml/datasets/vocab_reconstruct_oscar_normalized.txt

oscar_omop/vocab_oscar_reconstruct_expanded.txt
```

大型权重文件和 datasets 没有通过普通 Git 保存到远程仓库，而是使用 SCP 单独传输：

```text
oscar_omop/model.safetensors
aiml/datasets/*.parquet
```
