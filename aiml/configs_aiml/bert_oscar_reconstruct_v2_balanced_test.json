{
    "problem_type": "single_label_classification",
    "num_labels": 2,
    "target_column": "COHORT",
    "threshold": 0.5,
    "test_size": 0.05,
    "random_state": 42,
    "max_length": 512,
    "patience": 8,
    "data_filepath": "datasets/reconstruct_training_last_record_oscar_relative_cohort_clean.parquet",
    "test_data_filepath": "datasets/reconstruct_inference_last_record_oscar_relative_cohort_clean_balanced_1to1.parquet",
    "finetuned_model_name": "finetuned_oscar_reconstruct_v2",
    "pretrained_model_dir": "outputs/pretrain_oscar_reconstruct_v2/checkpoint-7500",
    "training_args": {
        "per_device_train_batch_size": 32,
        "per_device_eval_batch_size": 32,
        "gradient_accumulation_steps": 8,
        "num_train_epochs": 50,
        "learning_rate": 0.000003,
        "logging_dir": "outputs/finetune_oscar_reconstruct_v2/logs",
        "save_steps": 100,
        "logging_steps": 50,
        "eval_steps": 100,
        "warmup_steps": 0,
        "load_best_model_at_end": true,
        "output_dir": "outputs/finetune_oscar_reconstruct_v2/checkpoints",
        "metric_for_best_model": "eval_roc_auc_1_vs_rest",
        "greater_is_better": true,
        "save_total_limit": 5,
        "save_safetensors": false,
        "fp16": true,
        "report_to": "none"
    }
}
